{
  "plugins": ["prettier"],
  "rules": {
    "prettier/prettier": "error",
    "compiler-version": "off",
    "avoid-throw": "off",
    "avoid-suicide": "error",
    "avoid-sha3": "warn",
    "reason-string": "off",
    "func-visibility": ["warn", { "ignoreConstructors": true }]
  }
}
